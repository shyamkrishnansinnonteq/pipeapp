pipeline {
    agent any
    stages {
        stage('buld code') {
            steps {
                echo "build code"
            }
        }
        stage('deploy code') {
            steps {
                echo "deploy code"
            }
        }
    }
}